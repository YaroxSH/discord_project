Проект бота "архиватора" в дискорде. С помощью команд можно занести в базу данных то, что может поместиться в сообщении, после этого можно взаимодействовать с базой данных разными способами.
Список команд и того, что в них вводить:
-archivate отвечает за занесение сообщения в базу данных
!#archivate <имя сообщения> <число от 1 до 10 отображающее важность сообщения> <содержание сообщения>

-show может вывести сообщение из базы данных
!#show <тип вывода(rank, name, show, all)> <имя при типе name или приблизительный набор символов имени при типе search, в остальных случаях ничего не требуется>

-delete удаляет сообщение по имени
!#delete <имя сообщения>

-runback возвращает последнее удалённое сообщение
!#runback

-change позволяет изменять сообщение в базе данных
!#change <имя сообщения> <что нужно изменить(name, rank, context)> <на что заменять>
